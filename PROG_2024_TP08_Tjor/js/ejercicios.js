export function calcularEdad(fecha) {
    //return new Date(Date.now() - fechaNacimiento.valueOf()).getFullYear();
}

export function tirandoFruta(params) {
    const frutas = ["banana", "manzana", "pera", "anana", ""];
}

export function name(params) {
    
}